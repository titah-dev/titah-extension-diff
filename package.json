{
  "name": "@titah/extension-diff",
  "version": "0.1.0",
  "description": "Diff panel for Titah — git diff + session changes, tabbed view. Right side.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=22.6",
    "titah": "^0.4.0"
  },
  "titah": {
    "panel": "./dist/panel.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build --silent && node --test test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "titah-code": "^0.4.0",
    "typescript": "^5.9.0"
  },
  "keywords": [
    "titah",
    "titah-extension",
    "diff",
    "git"
  ]
}
